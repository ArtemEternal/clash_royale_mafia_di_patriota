{
  "name": "my-workspaces-powered-project",
  "private": true,
  "workspaces": ["frontend", "back"],
  "scripts": {
    "build": "yarn build --workspace=frontend", 
    "start": "yarn start --workspace=back"
  }
}
