{
  "name": "clash-royale-app",
  "version": "1.0.0",
  "scripts": {
    "build": "yarn --cwd frontend install --frozen-lockfile && yarn --cwd frontend build",
    "start": "node backend/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}